# Copy to .env for local use. Never publish API keys.
SEOUL_API_KEY=
OPENWEATHER_API_KEY=
# Public deployment only: use a random secret of 32+ characters.
# PUBLIC_DEMO=1
# SECRET_KEY=
# DATA_DIR=/data
# PORT=8765
