# 검증 기록 — 2026-09-17

- 새 핵심 테스트 16개 통과: 가상 매장 3종 실제 Prophet 학습, 데이터 부족 대체 통계, 모델 실패 대체 통계, 넓은 구간에서도 방향 표시, 성별·연령 별도 학습, 날짜·CSV 검증, 실제 매장 보호, 세션 데이터 분리, CORS, API 누락 처리, 날씨 응답 집계, 인구 응답 검증, 중복 시간대 스냅샷 제외.
- 브라우저에서 초기 설정 → 대학가 시연 → 내일 수요 계산 → 현재 상권/주고객층 화면 확인.
- 서울시 공식 sample 키로 광화문·덕수궁 API에 HTTP 200 응답 확인. 실제 반환 JSON 파싱 성공. HTTPS 8088은 지원되지 않아 공식 HTTP 접속 주소 사용.
- OpenWeatherMap은 실제 키 미제공으로 계정 호출 미검증. 응답 형식·한국 시간 집계·불완전 예보 처리는 자동 테스트로 확인.
- Railway 배포 설정 포함. 실제 Docker 빌드, 공개 배포, 운영 가용성은 아직 미검증.
- 기존 shop.db 보존. 새 demand-v2.db를 사용합니다.
